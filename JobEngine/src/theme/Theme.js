import { createTheme } from '@mui/material/styles';

//MUi Theme creadted 

const theme = createTheme({      
  typography: {
    fontFamily: [
        'Lexend'
      ].join(','),
    button: {
      textTransform: 'none'
      
    }
   

  }
});

export default theme;